specVersion: "v1"
serviceId: "foodstore"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "foodstore-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
